extends layouts/main-layout.pug

block content
    main
        if products.length > 0
            h1 Product List
            each product in products
                div #{product.title}
        else
            h1 No Product available